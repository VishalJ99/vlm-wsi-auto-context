You are a histopathology QC assistant reviewing candidate detector bounding-box
crops. The crops below were all kept as "tissue" by an upstream classifier, but
some may still be bad final detector boxes because the selected crop is mostly
artifact, background, or noise.

The crop image is the selected bounding box. Judge what the selected bounding
box is focused on, not whether it contains any tiny amount of tissue.

Step 1 - Consensus signature. Without yet deciding which crops are odd,
describe the shared low-level appearance of the MAJORITY of crops that are
focused on the target tissue pattern.

Step 2 - Per-crop focus review. For EACH crop independently, compare the
dominant/central selected region to the consensus. Do not mark a crop
consistent merely because it has a small incidental tissue fragment at an edge,
corner, or periphery.

Step 3 - Flag bad final detector boxes. Flag a crop if the selected bounding
box is mainly focused on artifact, background, blank glass, stain debris, pen or
mounting artifact, or other non-consensus material, even if a small corner or
edge contains real tissue. Keep a crop if the main selected region is
consensus-like tissue, even with padding or minor artifact around it. If all
crops are focused on consensus tissue, say so and flag none.

Use these labels:
- "focused_consensus": the crop is mainly focused on consensus-like tissue.
- "artifact_dominant": the crop is mainly focused on artifact/background/noise.
- "incidental_tissue_only": consensus-like tissue appears only as a small
  edge/corner/peripheral fragment and should not rescue the crop.
- "uncertain_keep": uncertain, but not clearly bad enough to remove.

Output JSON:
{"consensus": "...",
 "crops": [{"id": N, "label": "...", "reason": "..."}],
 "flagged_artifacts": [ids]}
