# 2026-04

## 2026-04-30 - PER-188 foreground auto-context pilot

Objective:
Run the no-ICL foreground auto-context pipeline on one easy H&E slide and one challenging Tol Blue slide, using Gemini Flash four-orientation TTA for Stage 1 bbox detection and local Qwen3-VL 8B via vLLM for Stage 6 patch classification.

Method:
- H&E input: `/data2/vj724/multistain/pilot_data/H&E/he_patient_003_slide_003.svs`.
- Tol Blue input: `/data2/vj724/multistain/pilot_data/TOL_BLUE/tol_blue_patient_054_slide_004.svs`.
- Initial commands capped both runs at Stage 3 while local vLLM startup was diagnosed.
- Moved Qwen cache from `/vol/biomedic3/vj724/hub` to `/data2/vj724/hf_cache`; see `/data2/vj724/hf_cache/reproduction.txt`.
- Resumed both runs through Stage 7 with `--stage6-icl-k 0`, no `--stage2-force-read-l0`, `--stage6-rotations 0`, and local vLLM at `http://localhost:8000/v1`.
- Reproduction files:
  - `/data2/vj724/vlm-wsi-auto-context/runs/auto_context_pilot/he_patient_003_slide_003/he_p003_s003_icl0_20260430_175702/reproduction.txt`
  - `/data2/vj724/vlm-wsi-auto-context/runs/auto_context_pilot/tol_blue_patient_054_slide_004/tol_blue_p054_s004_icl0_20260430_175702/reproduction.txt`

Observations:
- `/vol/biomedic3` behaved like the slow path for vLLM startup: model/env reads stalled in NFS wait before the server bound its port. Moving `HF_HOME` and `HF_HUB_CACHE` to `/data2` allowed Qwen to load and serve normally, though the conda env remains on `/vol/biomedic3`.
- H&E completed successfully through WSI-level Stage 7 at `2026-04-30T18:54:25.842693`; 3/3 bboxes completed Stage 6 and Stage 7.
- Tol Blue completed successfully through WSI-level Stage 7 at `2026-04-30T19:02:50.352485`; 3/3 bboxes completed Stage 6 and Stage 7.
- WSI-level overlays are:
  - `/data2/vj724/vlm-wsi-auto-context/runs/auto_context_pilot/he_patient_003_slide_003/he_p003_s003_icl0_20260430_175702/stage7/mask_overlay.png`
  - `/data2/vj724/vlm-wsi-auto-context/runs/auto_context_pilot/tol_blue_patient_054_slide_004/tol_blue_p054_s004_icl0_20260430_175702/stage7/mask_overlay.png`
- Reviewer batch discovery needed a small code fix because descriptive run IDs were not recognized as run directories.
- Reviewer attempts collected 6 tasks but failed: first due to stale `/data1` credential path, then due to Vertex 404 for `gemini-3-pro-preview` in project `ic-deep-learning-2026`.

Interpretation:
The foreground pipeline can run end to end in no-ICL mode using local Qwen for Stage 6 once the model cache is local. The next reliability issue is reviewer model access, not the segmentation pipeline itself.

Next steps:
- Choose an accessible reviewer model or route review through OpenRouter.
- Consider moving the vLLM/path-agent runtime off `/vol/biomedic3` if startup latency remains a recurring problem.
- Inspect H&E and Tol Blue overlays before using the runs as teacher data for distillation.

## 2026-04-30 - PER-188 high-resolution bbox reviewer inputs

Objective:
Make per-bbox reviewer inputs for the completed no-ICL H&E and Tol Blue runs without rerunning the foreground pipeline with `--stage2-force-read-l0`.

Method:
- Added `scripts/export_auto_context_reviewer_inputs.py`.
- Exporter reads each auto-context bbox from the source WSI using level-0 coordinates and a bounded pyramid-level read capped by `--max-dim`.
- Exporter rasterizes each `stage7/tissue_mask_post.npy` patch-grid mask onto the reviewer crop and writes Stage3-compatible `crop.png`, `mask.png`, `overlay.png`, and metadata.
- Exported both pilot runs with `--max-dim 2048 --padding-frac 0.02`.

Observations:
- H&E high-resolution reviewer inputs: `/data2/vj724/vlm-wsi-auto-context/runs/auto_context_reviewer_inputs/he_patient_003_slide_003/he_p003_s003_icl0_20260430_175702_stage7_l0_review`.
- Tol Blue high-resolution reviewer inputs: `/data2/vj724/vlm-wsi-auto-context/runs/auto_context_reviewer_inputs/tol_blue_patient_054_slide_004/tol_blue_p054_s004_icl0_20260430_175702_stage7_l0_review`.
- Dry-run discovery found 6 review tasks across both exported runs.
- Added OpenRouter/vLLM backend support to `run_vlm_reviewer_batch.py` so the reviewer is not limited to Vertex Gemini.

Next steps:
- Run `run_vlm_reviewer_batch.py` over `runs/auto_context_reviewer_inputs` with an approved model/provider.
