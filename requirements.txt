# Project-level Python requirements for the release-pruned foreground + reviewer method.
# Keep this as the canonical pip entrypoint; requirements_pipeline.txt contains detailed notes.
-r requirements_pipeline.txt
